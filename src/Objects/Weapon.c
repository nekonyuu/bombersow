#include <assert.h>

#include "Weapon.h"

Weapon* init_Weapon(int type)
{
    Weapon* new_weapon = NULL;
    assert(new_weapon = malloc(sizeof(Weapon)));

    // TODO

    return new_weapon;
}
